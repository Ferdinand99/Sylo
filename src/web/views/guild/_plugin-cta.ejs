<%# Swapped in for a plugin-grid "+ Enable" button once the module is on. The
    card's is-on class and the sidebar dot are updated by the moduleToggled
    listener in htmx-setup.js. %>
<a class="plugin-btn active" href="<%= href %>">✓ Active</a>
