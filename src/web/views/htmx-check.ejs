<%- include('partials/header', { title: 'Sylo — htmx check' }) %>

<h1 class="page-title">htmx foundation check</h1>
<p class="muted">Dev-only (Phase 0). Clicking <em>Bump</em> should swap the box below
  without a full page reload, pop a toast, and report that the CSRF header reached
  the server.</p>

<section class="card u-mw-md">
  <%- include('partials/htmx-check-body', { count: count, csrfSeen: csrfSeen }) %>
</section>

<p class="muted small" data-x-demo x-data="{ n: 0 }">
  Alpine check: <button type="button" @click="n++">clicked <span x-text="n"></span> times</button>
</p>

<%- include('partials/footer') %>
