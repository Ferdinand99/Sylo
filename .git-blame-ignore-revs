# Commits to skip in `git blame`. Bulk-formatting / mechanical rewrites only.
# Enable locally with:  git config blame.ignoreRevsFile .git-blame-ignore-revs
# (GitHub uses it automatically.)

# style: apply Prettier across the codebase  <-- add this commit's full SHA
