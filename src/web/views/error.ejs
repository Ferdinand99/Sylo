<%- include('partials/header', { title: `Sylo — ${typeof title !== 'undefined' ? title : 'Error'}` }) %>

<section class="card">
  <h2><%= typeof heading !== 'undefined' ? heading : 'Something went wrong' %></h2>
  <p class="muted"><%= typeof message !== 'undefined' ? message : 'Please try again.' %></p>
  <p><a href="/">&larr; Back to the dashboard</a></p>
</section>

<%- include('partials/footer') %>
