# Commits to skip in `git blame` — bulk-formatting / mechanical rewrites only,
# so line history points at the change that mattered.
#
# GitHub applies this automatically. Locally, run once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# chore: add ESLint + Prettier tooling and apply it across the codebase
0cf4b56e62460ac1ddeb18c884e5baa08f7a2011
